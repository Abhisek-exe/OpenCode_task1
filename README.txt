1.explain briefly the diffrence between git and git hub?
answer..git is a tool that is used to manage multiple versions of source code edits that are then transferred to files in a git repository.
git hub is a platform of online software devlopment which stores our data,and it serves as a location for uploading copies of a git repository..
2.expain briefly any five commands used in git?
 answer..a.git branch-creates a new branch.
         b.git checkout- switches to the specified branch and updates the working directory.
         c.git merge-combines the specified branch's history into the current branch .
         d.git init-turn an existing directory into a git repository.
         e.git clone-clone or download a repository that exists on github,including all of the files ,branches and commit.
3.how to make a pull request?
answer.. a.switch to the branch that you wnat to pull request for.
          b.click create pull request.
           c.on git hub, confirm that the branch in the base:drop down the menu in the branch where you want to merge your changes.
            d.type a title and description for your pull request
